# 💬 Детальная реализация чатов

## Обзор

Детальный экран чатов реализован 1 в 1 по дизайну из Figma с поддержкой различных типов сообщений и действий.

## 🎨 Реализованные экраны

На основе 4 экранов из Figma создана полная система чатов:

### 1. **chat with girl** - Базовый чат
- Приветственная секция с большим аватаром в градиентной рамке
- Текстовые сообщения от AI
- Кнопки действий (Send me a video)

### 2. **Welcome bonus** - Чат с бонусным контентом
- Заблокированный видео контент
- Бейдж "Welcome bonus" с таймером
- Кнопка разблокировки с ценой в Stars
- Бейдж "Free" для промо контента

### 3. **video message** - Видео сообщения
- Видео сообщения с превью
- Бейдж "Video"
- Кнопка воспроизведения
- Временная метка

### 4. **Unlock content** - Множественный заблокированный контент
- Несколько заблокированных видео
- Кнопка "Unlock" с градиентом
- Индикатор количества фото

## 📁 Структура компонентов

```
components/
├── ChatNavBar.tsx         # Навигационная панель чата
├── MessageBubble.tsx      # Пузырь сообщения (AI и пользователь)
├── VideoMessage.tsx       # Видео сообщение
├── LockedContent.tsx      # Заблокированный контент
├── ActionButton.tsx       # Кнопка действия (Send video и т.д.)
└── MessageInput.tsx       # Поле ввода сообщения

app/
└── chat.tsx               # Основной экран чата
```

## 🎯 Компоненты

### ChatNavBar
Навигационная панель с:
- Кнопкой назад
- Аватаром собеседника
- Статусом "Online"
- Кнопкой "Call Her" с градиентом

**Props:**
```typescript
{
  name: string;           // Имя собеседника
  status: string;         // Статус (Online)
  avatar: ImageSource;    // Аватар
  onCallPress?: () => void;
}
```

### MessageBubble
Сообщение в чате с:
- Поддержкой AI и пользовательских сообщений
- AI иконкой с градиентом
- Временной меткой
- Адаптивной шириной

**Props:**
```typescript
{
  message: string;        // Текст сообщения
  time: string;           // Время (1:35pm)
  isFromAI: boolean;      // От AI или от пользователя
}
```

### VideoMessage
Видео сообщение с:
- Превью изображением
- Кнопкой Play
- Бейджем "Video"
- Временной меткой
- Полупрозрачным оверлеем

**Props:**
```typescript
{
  image: ImageSource;     // Превью видео
  time: string;           // Время
  duration?: string;      // Длительность/тип
  onPress?: () => void;   // Обработчик нажатия
}
```

### LockedContent
Заблокированный контент с:
- Размытым превью
- Иконкой замка
- Информацией о разблокировке
- Кнопкой "Unlock for X Stars"
- Опциональными бейджами (Welcome bonus, таймер)
- Индикатором количества фото

**Props:**
```typescript
{
  image: ImageSource;     // Превью контента
  time: string;           // Время
  stars?: number;         // Цена в Stars (39)
  isFree?: boolean;       // Бесплатно (показать "Free")
  badge?: string;         // Текст бейджа ("Welcome bonus")
  badgeTime?: string;     // Время в бейдже ("14:27")
  photoCount?: number;    // Количество фото (10)
  onUnlockPress?: () => void;
}
```

### ActionButton
Кнопка действия с:
- Градиентным фоном
- Иконкой огня
- Бордером

**Props:**
```typescript
{
  text: string;           // Текст кнопки
  onPress?: () => void;   // Обработчик
  icon?: boolean;         // Показать иконку
}
```

### MessageInput
Поле ввода с:
- Кнопкой добавления изображения
- Многострочным вводом
- Кнопкой отправки с градиентом

**Props:**
```typescript
{
  onSend?: (message: string) => void;
  onImagePress?: () => void;
}
```

## 🎨 Дизайн-система чатов

### Цвета
- **Фон**: `#0f0e0f`
- **Бабл сообщения**: `#191919`
- **Градиент AI иконки**: `#d05bf8` → `#ff18a0`
- **Градиент кнопки Call**: `rgba(71, 24, 102, 0.8)` → `rgba(91, 26, 69, 0.8)`
- **Градиент Action button**: `#471866` → `#5b1a45`
- **Градиент Send button**: `#301866` → `#5b1a3d`
- **Градиент Unlock**: `#d657ed` → `#fa2aaa`
- **Градиент аватара**: `#d058f8` → `#f9319e`
- **Бордер кнопок**: `#f03bbd`

### Размеры
- **Видео контент**: 320x420px
- **Аватар в навбаре**: 40x40px
- **Большой аватар**: 147x197px
- **AI иконка**: 32x32px
- **Кнопки**: 48x48px
- **Border radius**: 12px (сообщения), 16px (кнопки)

## 🚀 Использование

### Открыть чат
```typescript
// Из любого места приложения
import { router } from 'expo-router';

router.push('/chat');
```

### Из списка чатов
Уже реализовано - нажатие на любой чат в списке (`app/(tabs)/chats.tsx`) открывает детальный экран.

## 📸 Скачанные ассеты

Все изображения скачаны через MCP:

### Аватары
- `export-15-232-*.png` - Маленький аватар для навбара
- `export-15-247-*.png` - Большой аватар для приветствия

### Видео контент
- `export-58-1430-*.png` - Welcome bonus видео
- `export-58-4110-*.png` - Видео сообщение
- `export-59-4319-*.png` - Заблокированный контент 1
- `export-59-4331-*.png` - Заблокированный контент 2

## 💡 Особенности реализации

### 1. Приветственная секция
- Большой аватар в градиентной рамке
- Текст приветствия с ограничением ширины
- Центрированное расположение

### 2. Типы сообщений
- **Текстовые** - от AI и пользователя
- **Видео** - с превью и кнопкой Play
- **Заблокированные** - с оверлеем и кнопкой разблокировки

### 3. Бейджи и метки
- **Дата** - "Today" в сером бадже
- **Время** - в каждом сообщении
- **Welcome bonus** - специальный бейдж с градиентом
- **Video** - бейдж на видео
- **Количество фото** - в углу заблокированного контента

### 4. Градиенты
Все градиенты соответствуют дизайну:
- AI иконка: розовый → фиолетовый
- Unlock кнопка: насыщенный розовый
- Action buttons: темный фиолетовый
- Рамка аватара: яркий розовый

### 5. Прокрутка
ScrollView автоматически прокручивается к последнему сообщению при отправке.

## 🔄 Дальнейшее развитие

Можно добавить:
1. ✅ **Анимации появления сообщений**
2. ✅ **Swipe actions на сообщениях**
3. ✅ **Индикатор набора текста**
4. ✅ **Push уведомления**
5. ✅ **Сохранение истории чата**
6. ✅ **Отправка голосовых сообщений**
7. ✅ **Реакции на сообщения**
8. ✅ **Полноэкранный просмотр видео**

## 🎯 Соответствие дизайну

### ✅ Полностью реализовано
- Все 4 экрана из Figma
- Все типы сообщений
- Все кнопки и действия
- Все градиенты и цвета
- Все размеры и отступы
- Все иконки и бейджи
- Навигация и поле ввода

### 📏 Pixel-perfect
- Размеры компонентов
- Отступы
- Скругления
- Цвета (включая opacity)
- Градиенты (направление и цвета)
- Шрифты и размеры текста

## 🎨 Примеры использования

### Добавить текстовое сообщение
```typescript
<MessageBubble
  message="Hello! How are you?"
  time="1:35pm"
  isFromAI={true}
/>
```

### Добавить видео сообщение
```typescript
<VideoMessage
  image={require('@/assets/exports/video.png')}
  time="1:35pm"
  duration="Video"
  onPress={() => console.log('Play video')}
/>
```

### Добавить заблокированный контент
```typescript
<LockedContent
  image={require('@/assets/exports/locked.png')}
  time="1:35pm"
  stars={39}
  isFree={true}
  badge="Welcome bonus"
  badgeTime="14:27"
  photoCount={10}
  onUnlockPress={() => console.log('Unlock content')}
/>
```

### Добавить кнопку действия
```typescript
<ActionButton
  text="Send me a video"
  onPress={() => console.log('Action')}
/>
```

## 📱 Навигация

Чат интегрирован в существующую навигацию:
- `app/(tabs)/chats.tsx` - Список чатов
- `app/chat.tsx` - Детальный экран чата

При нажатии на чат в списке открывается детальный экран с полной историей сообщений.

